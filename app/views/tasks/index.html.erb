<div class="container" style="min-height: 100vh; height: 100%;">
  <h1 class="text-center">Mike Services</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><i class="em <%= @icons.sample(1)[0].to_s %>"></i><%= task.description %> by: <%= task.user %> </li>
    <% end %>
  </ul>

  <br>
  <div class="footer navbar-fixed-bottom text-center" style="background-color: white; padding: 16px;box-shadow: 0px 0px 1em rgba(0,0,0,0.1); }">
    <style>
      input, input::placeholder {
        font-size: 12px;
      }
    </style>
    <div class="row"
    style="
    font-size: 12px !important;">
      <%= simple_form_for Task.new, html: { class: "" } do |f| %>
        <%= f.input :status, as: :hidden, input_html: { value: false } %>
      <div class="col-xs-6">
        <%= f.input :description, label: "¿Qué necesitas?", placeholder: "Ej: Una costeñita" %>
      </div>
      <div class="col-xs-6">
        <%= f.input :user, label: "¿Quién eres?", placeholder: "Ej: Mike Servicios" %>
      </div>
      <div class="col-xs-12">
        <%= button_tag type: 'submit', class: "btn btn-danger" do %>
          #HelpMeMike
          <i class="fas fa-medkit"></i>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>
</div>
